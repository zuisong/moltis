//! Event filtering logic (re-exported from types for clarity).
//!
//! The core `EventFilter::accepts()` method is on the type itself.
//! This module exists for the filter module reference in lib.rs.

// Filter logic is implemented directly on EventFilter in types.rs.
// This module is a placeholder for any future filter utilities.
